# SATIP Stream Recorder Environment Variables

# SATIP Stream URL (required for CLI mode, optional for web mode)
STREAM_URL=http://your-satip-server/?src=1&freq=11053&pol=h&ro=0.35&msys=dvbs2&mtype=8psk&plts=off&sr=22000&fec=34&sid=28429&pids=0,18,120,121

# Timezone for filename timestamps (optional, default: Europe/Vienna)
# Use IANA timezone names: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TIMEZONE=Europe/Vienna

# Web interface port (optional, default: 3000)
PORT=3000

# Output directory for recordings (optional, default: /recordings)
# This is the path INSIDE the container. The host path is set in docker-compose.yml volumes
OUTPUT_DIR=/recordings

# Local recordings directory path (for docker-compose volume mount)
# Change this to your desired local directory path
RECORDINGS_PATH=./recordings

