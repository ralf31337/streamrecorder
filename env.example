# SATIP Stream Recorder Environment Variables

# SATIP Stream URL (required)
# This can be overridden with --stream-url argument
STREAM_URL=http://xoro.localnet/?src=1&freq=11053&pol=h&ro=0.35&msys=dvbs2&mtype=8psk&plts=off&sr=22000&fec=34&sid=28429&pids=0,18,120,121

# Timezone for filename timestamps (optional, default: Europe/Vienna)
# Use IANA timezone names: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TIMEZONE=Europe/Vienna

# Note: DURATION and PREFIX are passed as command-line arguments, not environment variables
# Example: docker run satip-recorder 60 morning_show
#   - 60 = duration in minutes
#   - morning_show = filename prefix

